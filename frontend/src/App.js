// src/App.js

// Импорт базовых стилей приложения. Это всегда нужно, если они общие для всего приложения.
import "./App.css";

// Импортируем наш новый корневой компонент дашборда.
// Предполагается, что 'MyDashboard' теперь содержит всю необходимую логику
// для взаимодействия с Web3, контрактами, IPFS и авторизацией,
// или импортирует нужные функции и контексты внутрь себя.
import MyDashboard from "./components/Dashboard/MyDashboard";

/**
 * Главный компонент приложения.
 * Он отвечает за рендеринг верхнеуровневой структуры приложения.
 * В данном случае, он просто включает MyDashboard.
 */
function App() {
    return (
        <div className="App">
            {/*
        Рендерим компонент MyDashboard.
        Теперь все специфичные для дашборда импорты (Web3, контракты, контексты и т.д.)
        должны быть внутри MyDashboard.js или в тех компонентах, которые он импортирует.
      */}
            <MyDashboard />
        </div>
    );
}

export default App;
